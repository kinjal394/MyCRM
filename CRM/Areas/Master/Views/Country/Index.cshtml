
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div ng-controller="CountryController">

    <div class="row" data-ng-init="setpermission('@ViewBag.add','@ViewBag.edit','@ViewBag.delete','@ViewBag.view')">
        <div class="col-md-12">
            <!-- BEGIN PORTLET-->
            <div class="portlet light bordered">
                @*<div class="portlet-title">
                        <div class="caption">
                            <span class="caption-subject font-dark bold uppercase">Add Country</span>
                        </div>
                    </div>*@
                <div class="portlet-body">
                    <div>
                        <div>
                            @*<div>
                                    <input type="button" class="btn btn-default" href="@Url.Action("CountryPopup","Country")" data-toggle="modal" data-target="#myConfirmModal" id="btnCountry" value="Add Country" />
                                </div>*@
                            <div>
                                @*<button type="button" class="btn btn-sm btn-info" data-ng-click="Add(0)" tabindex="1">Add New Country</button>*@
                            </div>
                            <div class="row">
                                <div class="col-sm-12">
                                    <div ng-if="isadd==true"> <elaunchgrid data-grid-obj="gridObj" mode="CountryMaster" set-fn="setDirectiveRefresh(theDirFn)" title="Country List" onaddnew="Add(0)"></elaunchgrid></div>
                                    <div ng-if="isadd==false"> <elaunchgrid data-grid-obj="gridObj" mode="CountryMaster" is-add="false" set-fn="setDirectiveRefresh(theDirFn)" title="Country List" onaddnew="Add(0)"></elaunchgrid></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- END PORTLET-->
        </div>
    </div>
    <div id="myConfirmModal" class="modal fade" role="dialog" aria-labelledby="myModalLabel">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
            </div>
        </div>
    </div>
</div>
<script type="text/ng-template" id="myModalContent.html">
    <ng-include src="'@Url.Action("CountryPopup", "Country")'"></ng-include>
</script>
@section Scripts
{
    @Scripts.Render("~/bundles/ElaunchControls")
    @Scripts.Render("~/bundles/Country")


}


